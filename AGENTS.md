# quanta_maths Codex Agent Entrypoint

This repo uses `docs/thor-agent.md` as the canonical cross-agent operating guide.
Read this MD now.

If you are a named thread (e.g. "You are the `maths` thread"), also read your
thread-specific contract next: `docs/<thread>-agent.md` (e.g. `docs/maths-agent.md`).

Before doing any Colab, Hugging Face, long-running experiment, or research work,
check `git status`. Assume unrelated dirty files belong to the human or another
thread unless directly conflicting.
